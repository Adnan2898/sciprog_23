This practical involved use of recursive and iteration method to find the GCD of two numbers.
We have commented each line explaining how and what values are being used.

in order to run the file use the following code:

gcc -o runfile GCD_cal.c -Wall